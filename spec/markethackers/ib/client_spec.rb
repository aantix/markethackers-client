RSpec.describe Markethackers::Ib::Client do
  it "has a version number" do
    expect(Markethackers::Ib::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
